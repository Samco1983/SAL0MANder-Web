{
  "//": "DRAFT — NOT DEPLOYED. Owner: Gemini (cloud lane). Indexes kept as handed over; one TTL override removed — see infra/FIRESTORE-REVIEW.md item 5.",
  "indexes": [
    {
      "collectionGroup": "activities",
      "queryScope": "COLLECTION",
      "fields": [
        { "fieldPath": "shareCode", "order": "ASCENDING" },
        { "fieldPath": "isPublished", "order": "ASCENDING" }
      ]
    },
    {
      "collectionGroup": "activities",
      "queryScope": "COLLECTION",
      "fields": [
        { "fieldPath": "ownerId", "order": "ASCENDING" },
        { "fieldPath": "updatedAt", "order": "DESCENDING" }
      ]
    },
    {
      "collectionGroup": "gameplay_sessions",
      "queryScope": "COLLECTION",
      "fields": [
        { "fieldPath": "activityId", "order": "ASCENDING" },
        { "fieldPath": "startedAt", "order": "DESCENDING" }
      ]
    },
    {
      "collectionGroup": "gameplay_sessions",
      "queryScope": "COLLECTION",
      "fields": [
        { "fieldPath": "playerId", "order": "ASCENDING" },
        { "fieldPath": "startedAt", "order": "DESCENDING" }
      ]
    },
    {
      "collectionGroup": "generation_batches",
      "queryScope": "COLLECTION",
      "fields": [
        { "fieldPath": "ownerId", "order": "ASCENDING" },
        { "fieldPath": "createdAt", "order": "DESCENDING" }
      ]
    }
  ],
  "fieldOverrides": [
    {
      "collectionGroup": "generation_batches",
      "fieldPath": "expiresAt",
      "ttl": true
    },
    {
      "//": "Agreed TTL topology: the root /gameplay_sessions/{id} document carries NO TTL, so teacher history is permanent. Trace events live in the telemetry subcollection and expire there, which is the only way a 30-day TTL can prune traces without deleting the session record with them.",
      "collectionGroup": "telemetry",
      "fieldPath": "expiresAt",
      "ttl": true
    }
  ]
}
