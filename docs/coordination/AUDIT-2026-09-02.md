# Full audit — where every thread stands

**2026-09-02 · everything discussed, with owner and status**

---

## A. Shipped and verified — web lane

All on `feat/three-demo-activities`, **PR #95, MERGEABLE, not merged.**
None of it is live until that merges.

| # | Item | Evidence |
| --- | --- | --- |
| A1 | Three activities keyed to Unity's real ids | Rendered from `MOCK_DEMO_ACTIVITIES`; ids pinned in `threeDemoActivities.test.ts` |
| A2 | Unity no longer runs behind a 404 | Browser-verified: bad id gives 0 canvases, 0 loader scripts |
| A3 | Home page offers all three, each to its own bundle | `act_one_step_inequalities` → its own `-v1` bundle |
| A4 | Share links return 200 instead of 404 | `prerender-routes.mjs` emits three `/play/*/index.html` |
| A5 | robots.txt / sitemap contradiction fixed | Three `Allow:` lines; mutation-tested guard |
| A6 | Puzzle art on the site | 14 generated → 12 usable → 13.9 MB compressed to 932 KB |
| A7 | Visible scrollbar site-wide | 14px, green, matches the Unity tray; measured 0px → 14px |
| A8 | Trust pages (about / privacy / terms) | Merged earlier; 200, crawlable, no compliance claims |
| A9 | Fullscreen for the game stage | Merged earlier |

**Suite: 852 tests green, build green.**

## B. Shipped — Unity lane (Codex)

| # | Item | Where |
| --- | --- | --- |
| B1 | Drag fix — a snapped piece locks | `PuzzlePiece.cs`, verified in the pushed commit |
| B2 | Piece tray scrollbar | Unity PR #23 |
| B3 | Teacher Studio redesign, 484 lines | `5ac591c` — **not to the owner's wireframe**, see §E1 |
| B4 | Auto-place / Mystery Reveal engine support | `b0eb887`, 2026-08-30. On for all three demos |
| B5 | Recovery branch backed up to GitHub | `86a8dce`, pushed 2026-09-01 |
| B6 | Fresh WebGL build produced | 93 MB, local only |

## C. Decided

| # | Decision | By |
| --- | --- | --- |
| C1 | **Piece count is 9** | Owner |
| C2 | **Palette resolved** — one hue pair, lightness steps per surface; emerald `#38A169` stays a status colour, not brand | Owner |
| C3 | **The per-answer reward modal is removed** — inline snap, 600–800 ms, nothing to press | Owner |
| C4 | Unity owns Teacher Studio and Student Play; the web lane builds neither | Owner |
| C5 | Backend must be a platform API — bearer tokens, one generated contract, share-link URLs frozen | Owner framing |
| C6 | Guest Play stays ungated on every platform | CLAUDE.md non-negotiable 3 |

## D. Open decisions — owner's call

| # | Question | Recommendation |
| --- | --- | --- |
| D1 | Accuracy % on the completion screen | **Cut it.** A student who has "already decided math is not for them" finishing at 58% will not press Play Again. Keep `12/12` |
| D2 | Timer counts up or down? | Down is classroom pressure; up is a record. Confirm intent |
| D3 | Text size A−/A/A+ — spec files it as "future" | **Promote it.** One of the two live complaints |
| D4 | Districts page — written, held | Owner said "why not just protect ourselves". Still held |
| D5 | Backend / auth provider | Nothing chosen. Constraints written up in `DECISION-INPUT-BACKEND-IS-A-PLATFORM-API.md` |

## E. Open work — Unity lane

| # | Item | Note |
| --- | --- | --- |
| E1 | **Teacher Studio to the wireframe** | 9 gaps: five tabs, Readiness Checklist, Subject, Grade Level, Publish gate, Activity Summary, Quick Actions, Recent Changes, Activity Notes. Spec: `SPEC-TEACHER-STUDIO-ACTIVITY-EDITOR.md` |
| E2 | **Merge PR #23, close #22** | #23 strictly contains #22. Merging #22 does **not** deliver the drag fix |
| E3 | **Rebuild WebGL from #23 and hand over four files** | The only path to a playable live site. `RUNBOOK-SHIP-A-UNITY-BUILD.md` |
| E4 | Remove the per-answer modal (C3) | Inline snap using the existing glow |
| E5 | Contrast fixes | White on `CONTINUE` green measures **1.21:1**. Dark text on the same green is 15.96:1 |
| E6 | Name Mystery Reveal as an Activity Type | Engine already supports it; only the label is missing |
| E7 | Correct both wireframes to 9 pieces | They read 24 and 12 |
| E8 | `.gitattributes` — `*.unity binary` blocks all scene merges | Root cause of branch serialisation. UnityYAMLMerge config in `UNITY-REPO-DIAGNOSIS-2026-09-01.md` |
| E9 | Verify `autoPlaceCorrectPieces` reaches the build | If auto-place is on for the demos, drag is not the critical path. Cheaper to check than to rebuild |

## F. Open work — web lane

| # | Item | Blocked on |
| --- | --- | --- |
| F1 | Merge PR #95 | Owner |
| F2 | `piecePlacement` contract field for Mystery Reveal | Codex accepting the proposed delta |
| F3 | `DistrictsPage.test.tsx` fails — `getByText('sal0mander.com')` matches multiple | Not the web lane's file; held page, another agent editing |
| F4 | Flaky test in `routing.test.tsx` | Passed 852/852 twice after; low priority |
| F5 | Four unused library images shipped in `public/` | Deliberate — a library for the next surface. 358 KB |

## G. Owner actions — nobody else can do these

| # | Action |
| --- | --- |
| G1 | `gh pr merge 95 --squash --delete-branch` |
| G2 | Call Lightspeed, 1-877-447-6244, central time, as the site owner |
| G3 | Submit the domain to Securly, Cisco Talos, FortiGuard |
| G4 | Decide D1–D5 |

## H. Risks and things not to do

| # | Risk |
| --- | --- |
| H1 | **Do not send Antigravity's filter report to a district.** It claims COPPA/FERPA compliance — which the owner explicitly forbade without evidence — and claims "0 pre-drawn puzzle cut lines", which is false |
| H2 | Two generated images have jigsaw cut lines painted into the pixels: `panther_chameleon_rainforest`, `robot_alien_crystals`. Excluded, and named in a test. **The next art prompt must say: no puzzle overlays, no cut lines** |
| H3 | Merging Unity PR #22 believing it carries the drag fix. It does not |
| H4 | Building Unity from the working tree rather than a branch — it currently matches no commit |
| H5 | Any privacy/district page claim that the browser contacts one domain becomes false the day a backend on another host ships. Three pages need updating in that same change |

---

## The short version

Two merges stand between today and a working product:

1. **`gh pr merge 95`** — owner, ten seconds
2. **Unity PR #23, then rebuild** — Codex

Everything in §A is finished and invisible until (1). Everything a student
touches is broken until (2).
