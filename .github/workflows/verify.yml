name: verify

on:
  pull_request:
    branches: [main]
  push:
    branches: [main]
  workflow_dispatch:

# Verification only reads source and dependency metadata. It must not write to
# the repo, publish Pages, or mint deployment tokens.
permissions:
  contents: read

concurrency:
  group: verify-${{ github.ref }}
  cancel-in-progress: true

jobs:
  verify:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: 24
          cache: npm

      - run: npm ci

      - name: full local gate
        run: npm run verify

      - name: deploy-path build
        env:
          VITE_BASE_PATH: /SAL0MANder-Web/
        run: npm run build

      - name: Pages fallbacks and public route entry points
        run: npm run pages:fallback

      - name: deploy artifact gate
        run: npm run verify:artifact
